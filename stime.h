class STime {
public:
    int dow;
    float start;
    float stop;

    STime(int _dow, float _start, float _stop) {
        dow = _dow;
        start = _start;
        stop = _stop;
    }    
};